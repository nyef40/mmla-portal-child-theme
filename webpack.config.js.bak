// webpack.config.js
const path = require('path');

module.exports = {
  entry: './wordpress/wp-content/src/index.js',
  output: {
    path: path.resolve(__dirname, 'wordpress/wp-content/themes/blocksy-child/portal/assets/js'),
    filename: 'portal-app.js',
  },
  module: {
    rules: [
      {
        test: /\.(js|jsx)$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env', '@babel/preset-react']
          }
        }
      }
    ]
  },
  resolve: {
    extensions: ['.js', '.jsx']
  }
};